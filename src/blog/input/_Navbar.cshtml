<ul class="navbar-nav ml-auto">
    <li class="dropdown nav-item"> 
                                        <a href='@Context.GetLink("/projects")' class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Projects<span class="caret"></span></a>
                                        <ul class="dropdown-menu">
                                            <li class="nav-item"><a href="https://noknok.pl" class="nav-link">LuxBus</a></li>
                                            <li role="separator" class="divider"></li>
                                            <li class="nav-item"><a href="https://noknok.pl" class="nav-link">NokNok</a></li>
                                            <li role="separator" class="divider"></li>
                                            <li class="nav-item"><a href='@Context.GetLink("/warkenbeerfestival2023")' class="nav-link">Warken</a></li>
                                        </ul>
    </li>
    @foreach(IDocument doc in OutputPages.GetChildrenOf("index.html").Where(x => x.GetBool("ShowInNavbar", true)).OrderBy(x => x.GetInt(Keys.Order)))
    {
        // Don't add the home page to the nav bar
        if (doc.Destination != "index.html")
        {
            <li class="nav-item">
                @Html.DocumentLink(doc, new Dictionary<object, object> { { "class", "nav-link" } })
            </li>
        }
    }
</ul>