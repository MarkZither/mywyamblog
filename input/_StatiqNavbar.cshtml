<li class="dropdown"> 
                                        <a href="/projects" class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Projects<span class="caret"></span></a>
                                        <ul class="dropdown-menu">
                                            <li><a href="https://noknok.pl">LuxBus</a></li>
                                            <li role="separator" class="divider"></li>
                                            <li><a href="https://noknok.pl">NokNok</a></li>
                                        </ul>
</li>
@{
    /*List<Tuple<string, string>> pages = Context
        .Documents["Pages"]
        //.Where(x => x.Bool(Wyam.Blog.BlogKeys.ShowInNavbar, true))
        //.Where(x => x.FilePath(Keys.RelativeFilePath)?.FullPath?.StartsWith("offline") == (bool?)false)
        //.Where(x => x.FilePath(Keys.RelativeFilePath)?.FullPath?.StartsWith("index") == (bool?)false)
        .Select(x => Tuple.Create(x.WithoutSettings.String(Keys.Title), Context.GetLink(x)))
        .Where(x => !string.IsNullOrEmpty(x.Item1))
        .OrderBy(x => x.Item1)
        .ToList();
    if(Documents["Tags"].Any())
    {
        pages.Insert(0, Tuple.Create("Tags", Context.GetLink("tags/index.html")));
    }
    if(Documents["BlogPosts"].Any() && Context.Bool(Wyam.Blog.BlogKeys.GenerateArchive))
    {
        pages.Insert(0, Tuple.Create("Archive", Context.GetLink(Context.GetString("PostsPath") + "/index.html")));
    }
    foreach(Tuple<string, string> _page in pages)
    {
        <li><a href="@_page.Item2">@_page.Item1</a></li>
    }*/
}