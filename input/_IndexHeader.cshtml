<div class="site-heading">
    @if(Context.String(Wyam.Blog.BlogKeys.Description) != null)
    {
        <h1>@Context.String(Wyam.Blog.BlogKeys.Description)</h1>
    }
    @if(Context.String(Wyam.Blog.BlogKeys.Intro) != null)
    {            
        @if(Context.String(Wyam.Blog.BlogKeys.Description) != null)
        {
            <hr class="small">
        }
        <span class="subheading">@Context.String(Wyam.Blog.BlogKeys.Intro)</span>
    }
</div>